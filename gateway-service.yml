server:
  port: 9100

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8761/eureka/

spring:
  cloud:
    gateway:
      routes:     
        - id: customer-service
          uri: lb://customer-service
          predicates:
            - Path=/api/customer/**
