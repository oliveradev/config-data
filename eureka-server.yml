server:
  port: 8761
spring:
  application:
    name: eureka-server
eureka:
  client:
    register-with-eureka: false
    fetch:
        registry: false
    appname: eureka-server
    wait-time-in-ms-when-sync-empty: 0
